from collections import deque

'''
A queue can implemented in Python using either...
- a list
- collections.deque object
- queue.Queue object

Basic operations include:
        - enequeue() -> Adds an item to the queue
        - dequeue() -> Removes an element from the queue in order they were added (FiFo)
        - front() -> Returns the first (front) item in the queue
        - rear() -> Returns the last item from the queue
'''